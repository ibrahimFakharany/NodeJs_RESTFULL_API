#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/coffee" ]; then
  "$basedir/coffee"  "$basedir/../gcontacts/bin/gcontacts" "$@"
  ret=$?
else 
  coffee  "$basedir/../gcontacts/bin/gcontacts" "$@"
  ret=$?
fi
exit $ret
